{
  "name": "mongodb-display-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Vue.jsから利用するMongoDBデータ表示用Express API",
  "main": "app.js",
  "scripts": {
    "start": "node ./bin/www",
    "seed": "node ./scripts/seed.js",
    "check": "node --check app.js && node --check ./bin/www && node --check ./db/mongo.js && node --check ./routes/index.js && node --check ./routes/notes_from_b.js && node --check ./scripts/seed.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongodb": "^6.21.0"
  }
}
